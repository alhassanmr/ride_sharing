server.port=9000
# Spring Datasource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ride_sharing_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.initialization-mode=always

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Spring Security Configuration
# Add your security configuration here if using Spring Security

# Spring Websocket Configuration
# Add WebSocket-related configuration if using Spring Websocket

# Spring Kafka Configuration
# Add Kafka-related configuration if using Apache Kafka

# Redis Configuration
# Add Redis-related configuration if using Redis

# Custom Application Properties
custom.property=value
